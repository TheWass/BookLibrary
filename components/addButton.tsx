import React from 'react';
import { Button, Icon } from 'native-base';

const AddButton = () => {
    return (
        <Button transparent>
            <Icon name='add' />
        </Button>
    );
}

export default AddButton;